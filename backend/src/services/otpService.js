// backend/src/service/otpService.js
// Placeholder for future custom OTP (currently Firebase manages OTP)

export const sendCustomOTP = async (phone) => {
  // Example: integrate Fast2SMS / Twilio later
  console.log(`Simulated OTP sent to ${phone}`);
};
